# ============================================================================
# Environment Variables Template for Local Development
# ============================================================================
# Copy this file to .env and modify values as needed:
#   cp .env.example .env    (Linux/Mac)
#   copy .env.example .env  (Windows)
#
# The .env file is ignored by git for security reasons.
# ============================================================================

# Database Configuration
DB_NAME=ecommerce
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432

# Application Ports
API_PORT=7246
WEB_PORT=5173

# ASP.NET Core Environment
ASPNETCORE_ENV=Development

# Stripe Payment Configuration (REQUIRED for payment features)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# How to get Stripe keys:
# 1. Go to https://dashboard.stripe.com/test/apikeys
# 2. Copy Publishable key (pk_test_...)
# 3. Copy Secret key (sk_test_...)
# 4. For webhook secret, run: stripe listen --forward-to http://localhost:8080/api/webhook/stripe
# 5. Copy the webhook secret (whsec_...) from the output

# Cloudinary Image Upload Configuration (OPTIONAL)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ============================================================================
# NOTES:
# - For local development, these default values are sufficient
# - For production, use your hosting platform's environment variables
# - Never commit .env file to version control
# - Change DB_PASSWORD to a strong password for production use
# - Stripe keys are REQUIRED for checkout and payment features
# - Webhook secret is REQUIRED for automatic payment status updates
# ============================================================================
